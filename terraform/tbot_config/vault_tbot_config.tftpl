version: v2
proxy_server: ${proxy_server}
onboarding:
  join_method: gcp
  token: ${token_name}
storage:
  type: memory
services:
- type: application-tunnel
  app_name: ${app_name}
  listen: ${listen_addr}